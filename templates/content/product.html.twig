{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<section class="container">
		<h1>{{ product.title }}</h2>
		<p>{{ product.description }}</p>
		<p>{{ product.price }}
			€</p>
		{# <p>{{ product.updatedat|date("d/m/Y h:m") }}</p> #}
	</section>
{% endblock %}
